diff --git a/src/DLinkLiveControlPolicy.hpp b/src/DLinkLiveControlPolicy.hpp
new file mode 100644
--- /dev/null
+++ b/src/DLinkLiveControlPolicy.hpp
@@ -0,0 +1,13 @@
+#ifndef DLINK_LIVE_CONTROL_POLICY_HPP
+#define DLINK_LIVE_CONTROL_POLICY_HPP
+
+inline constexpr bool dlinkAudioWorkerRequired(bool configured_enabled) {
+  (void)configured_enabled;
+  return true;
+}
+
+inline constexpr bool dlinkLiveAudioRestartRequired() { return false; }
+
+inline constexpr bool dlinkMotionVideoRestartRequired() { return false; }
+
+#endif
diff --git a/src/globals.hpp b/src/globals.hpp
--- a/src/globals.hpp
+++ b/src/globals.hpp
@@ -319,6 +319,7 @@
 extern bool global_restart_rtsp;
 extern bool global_restart_video;
 extern bool global_restart_audio;
+extern bool global_restart_motion;
 
 extern bool global_osd_thread_signal;
 extern bool global_reload_osd;
diff --git a/src/main.cpp b/src/main.cpp
--- a/src/main.cpp
+++ b/src/main.cpp
@@ -5,6 +5,7 @@
 #include "Config.hpp"
 #include "ConfigWatcher.hpp"
 #include "DLinkMediaProcessPolicy.hpp"
+#include "DLinkLiveControlPolicy.hpp"
 /* DayNightWorker removed — photosensing delegated to daynightd */
 #include "IMPBackchannel.hpp"
 #include "IMPSystem.hpp"
@@ -61,6 +62,7 @@
 bool global_restart_rtsp = false;
 bool global_restart_video = false;
 bool global_restart_audio = false;
+bool global_restart_motion = false;
 bool global_main_thread_signal = false;
 bool global_motion_thread_signal = false;
 std::atomic<char> global_rtsp_thread_signal{1};
@@ -907,7 +909,13 @@ int main(int argc, const char *argv[]) {
     // before the audio output thread initialises.  On platforms with a
     // shared AI/AO CODEC clock (T10/T20/T21) the AO's
     // IMP_AO_GetPubAttr will then return the actual running rate.
-    if (cfg->audio.input_enabled && (global_restart_audio || startup)) {
+    bool audio_input_required = cfg->audio.input_enabled;
+    bool audio_output_required = cfg->audio.output_enabled;
+#if defined(DCS_DLINK_MEDIA_READY)
+    audio_input_required = dlinkAudioWorkerRequired(audio_input_required);
+    audio_output_required = dlinkAudioWorkerRequired(audio_output_required);
+#endif
+    if (audio_input_required && (global_restart_audio || startup)) {
       StartHelper sh{0};
       int ret =
           pthread_create(&global_audio[0]->thread, nullptr,
@@ -917,7 +925,7 @@ int main(int argc, const char *argv[]) {
       sh.has_started.acquire();
     }
 
-    if (cfg->audio.output_enabled && (global_restart_audio || startup)) {
+    if (audio_output_required && (global_restart_audio || startup)) {
       StartHelper ao_sh{0};
       int ret = pthread_create(&audio_output_thread, nullptr,
                                AudioOutputWorker::thread_entry,
@@ -927,9 +935,12 @@ int main(int argc, const char *argv[]) {
       // The IMP SDK shares internal state between AO and encoder
       // subsystems; concurrent init causes memory corruption.
       ao_sh.has_started.acquire();
+#if defined(DCS_DLINK_MEDIA_READY)
+      AudioOutputWorker::applyMute(!cfg->audio.output_enabled);
+#endif
     }
 
-    if (cfg->audio.output_enabled && (global_restart_audio || startup)) {
+    if (audio_output_required && (global_restart_audio || startup)) {
       StartHelper bc_sh{0};
       int ret = pthread_create(&backchannel_thread, nullptr,
                                BackchannelWorker::thread_entry,
@@ -974,16 +985,16 @@ int main(int argc, const char *argv[]) {
       if (cfg->osd.sei.enabled || cfg->osd.burnin.enabled) {
         int ret = pthread_create(&osd_thread, nullptr, OSD::thread_entry, NULL);
         LOG_DEBUG_OR_ERROR(ret, "create osd thread");
-      }
-
-      if (cfg->motion.enabled) {
-        int ret = pthread_create(&motion_thread, nullptr, Motion::run, &motion);
-        LOG_DEBUG_OR_ERROR(ret, "create motion thread");
       }
 
       /* daynight photosensing thread removed — use daynightd daemon */
     }
 
+    if (cfg->motion.enabled && (global_restart_motion || startup)) {
+      int ret = pthread_create(&motion_thread, nullptr, Motion::run, &motion);
+      LOG_DEBUG_OR_ERROR(ret, "create motion thread");
+    }
+
     // start rtsp server
     if (global_rtsp_thread_signal != 0 && (global_restart_rtsp || startup)) {
       int ret = pthread_create(&rtsp_thread, nullptr, RTSP::run, &rtsp);
@@ -997,10 +1008,11 @@ int main(int argc, const char *argv[]) {
     global_restart = false;
     global_restart_video = false;
     global_restart_audio = false;
+    global_restart_motion = false;
     global_restart_rtsp = false;
 
     while (!global_restart_rtsp && !global_restart_video &&
-           !global_restart_audio &&
+           !global_restart_audio && !global_restart_motion &&
            !global_shutdown_requested.load(std::memory_order_relaxed))
       global_cv_worker_restart.wait(lck);
 
@@ -1010,6 +1022,7 @@ int main(int argc, const char *argv[]) {
       global_restart_rtsp = true;
       global_restart_video = true;
       global_restart_audio = true;
+      global_restart_motion = true;
     }
     lck.unlock();
 
@@ -1061,14 +1074,17 @@ int main(int argc, const char *argv[]) {
       LOG_DEBUG_OR_ERROR(ret, "join backchannel thread");
     }
 
-    if (global_restart_video) {
-      // stop motion thread
+    if (global_restart_video || global_restart_motion) {
+      // Motion has its own bounded restart path on D-Link. It must not force
+      // the video/ISP process replacement used by structural stream changes.
       if (global_motion_thread_signal) {
         global_motion_thread_signal = false;
         int ret = pthread_join(motion_thread, NULL);
         LOG_DEBUG_OR_ERROR(ret, "join motion thread");
       }
+    }
 
+    if (global_restart_video) {
       // stop osd thread
       if (global_osd_thread_signal) {
         global_osd_thread_signal = false;
diff --git a/src/JsonAPI.cpp b/src/JsonAPI.cpp
--- a/src/JsonAPI.cpp
+++ b/src/JsonAPI.cpp
@@ -1,6 +1,7 @@
 #include "JsonAPI.hpp"
 #include "AudioOutputWorker.hpp"
 #include "Config.hpp"
+#include "DLinkLiveControlPolicy.hpp"
 #include "MP4Recorder.hpp"
 #include "globals.hpp"
 #include "imp_hal.hpp"
@@ -552,9 +553,25 @@ void handle_audio(JsonValue *obj, std::string &out, bool &sep) {
     if (JsonValue *v = obj_get(obj, key)) {
       if (v->type == JSON_BOOL) {
         cfg->set<bool>(path, v->value.boolean != 0);
-        if (rest_audio)
+        bool live_audio_restart = rest_audio;
+        bool live_rtsp_restart = rest_rtsp;
+#if defined(DCS_DLINK_MEDIA_READY)
+        bool live_toggle = strcmp(key, "mic_enabled") == 0 ||
+                           strcmp(key, "spk_enabled") == 0;
+        if (live_toggle && !dlinkLiveAudioRestartRequired()) {
+          live_audio_restart = false;
+          live_rtsp_restart = false;
+        }
+        if (strcmp(key, "spk_enabled") == 0) {
+          bool enabled = v->value.boolean != 0;
+          if (!enabled)
+            AudioOutputWorker::clearQueue(false);
+          AudioOutputWorker::applyMute(!enabled);
+        }
+#endif
+        if (live_audio_restart)
           global_restart_audio = true;
-        if (rest_rtsp)
+        if (live_rtsp_restart)
           global_restart_rtsp = true;
       }
       add_key(out, s2, key);
@@ -726,8 +743,16 @@ void handle_motion(JsonValue *obj, std::string &out, bool &sep) {
     if (JsonValue *v = obj_get(obj, key)) {
       if (v->type == JSON_BOOL) {
         cfg->set<bool>(path, v->value.boolean != 0);
-        if (restart_video)
+        if (restart_video) {
+#if defined(DCS_DLINK_MEDIA_READY)
+          if (dlinkMotionVideoRestartRequired())
+            global_restart_video = true;
+          else
+            global_restart_motion = true;
+#else
           global_restart_video = true;
+#endif
+        }
       }
       add_key(out, s2, key);
       add_bool(out, cfg->get<bool>(path));
@@ -1542,6 +1567,7 @@ bool handle_json(const char *input, std::string &out) {
   bool prev_restart_rtsp = global_restart_rtsp;
   bool prev_restart_video = global_restart_video;
   bool prev_restart_audio = global_restart_audio;
+  bool prev_restart_motion = global_restart_motion;
 
   // Iterate top-level keys
   for (JsonKeyValue *kv = root->value.object_head; kv; kv = kv->next) {
@@ -1589,7 +1615,8 @@ bool handle_json(const char *input, std::string &out) {
   // Notify main thread if any restart flags were set
   if (global_restart_rtsp != prev_restart_rtsp ||
       global_restart_video != prev_restart_video ||
-      global_restart_audio != prev_restart_audio) {
+      global_restart_audio != prev_restart_audio ||
+      global_restart_motion != prev_restart_motion) {
     global_cv_worker_restart.notify_one();
   }
 
diff --git a/src/RTSP.cpp b/src/RTSP.cpp
--- a/src/RTSP.cpp
+++ b/src/RTSP.cpp
@@ -1,4 +1,5 @@
 #include "RTSP.hpp"
+#include "DLinkLiveControlPolicy.hpp"
 #include "simple-rtsp/RtspServer.hpp"
 #include "simple-rtsp/RtspTypes.hpp"
 #include "Config.hpp"
@@ -75,7 +76,11 @@ void RTSP::addSubsession(int chnNr, const stream_attributes &stream) {
     server_->addVideoStream(chnNr, vcfg, global_video[chnNr]);
 
     // ── Audio stream ──────────────────────────────────────────────────
-    if (!audioConfigured_ && cfg->audio.input_enabled && stream.audio_enabled
+    bool audioInputAvailable = cfg->audio.input_enabled;
+#if defined(DCS_DLINK_MEDIA_READY)
+    audioInputAvailable = dlinkAudioWorkerRequired(audioInputAvailable);
+#endif
+    if (!audioConfigured_ && audioInputAvailable && stream.audio_enabled
         && global_audio[0] && global_audio[0]->imp_audio) {
         simple_rtsp::AudioStreamConfig acfg;
         int hwRate = global_audio[0]->imp_audio->sample_rate;
@@ -135,7 +140,11 @@ void RTSP::start() {
     // Backchannel is negotiated via ANNOUNCE (client sends SDP), not
     // advertised in DESCRIBE.  This keeps player clients (mpv, ffplay)
     // from trying to SETUP recvonly tracks.
-    if (cfg->audio.output_enabled) {
+    bool backchannelAvailable = cfg->audio.output_enabled;
+#if defined(DCS_DLINK_MEDIA_READY)
+    backchannelAvailable = dlinkAudioWorkerRequired(backchannelAvailable);
+#endif
+    if (backchannelAvailable) {
         server_->enableBackchannel();
     }
 
diff --git a/tests/test_media_queue.cpp b/tests/test_media_queue.cpp
--- a/tests/test_media_queue.cpp
+++ b/tests/test_media_queue.cpp
@@ -3,6 +3,7 @@
 #include "FrameRecoveryGate.hpp"
 #include "EncoderReceivePolicy.hpp"
 #include "DLinkMediaProcessPolicy.hpp"
+#include "DLinkLiveControlPolicy.hpp"
 #include "HTTPBindPolicy.hpp"
 #include "HTTPClientAdmission.hpp"
 #include "JpegDemandPolicy.hpp"
@@ -163,6 +164,13 @@ static void test_dlink_media_process_policy() {
          DLinkMediaProcessAction::Exit);
 }
 
+static void test_dlink_live_control_policy() {
+  assert(dlinkAudioWorkerRequired(false));
+  assert(dlinkAudioWorkerRequired(true));
+  assert(!dlinkLiveAudioRestartRequired());
+  assert(!dlinkMotionVideoRestartRequired());
+}
+
 static void test_socket_peer_disconnect_detection() {
   int sockets[2] = {-1, -1};
   assert(socketpair(AF_UNIX, SOCK_STREAM, 0, sockets) == 0);
@@ -384,6 +392,7 @@ int main() {
   test_video_receive_demand_and_transition_policy();
   test_stream_runtime_fps_policy();
   test_dlink_media_process_policy();
+  test_dlink_live_control_policy();
   test_socket_peer_disconnect_detection();
   test_exact_byte_boundary();
   test_exact_element_boundary();
