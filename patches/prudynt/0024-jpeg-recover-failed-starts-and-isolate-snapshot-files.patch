diff --git a/src/JPEGWorker.cpp b/src/JPEGWorker.cpp
index 3dffa99..410951f 100644
--- a/src/JPEGWorker.cpp
+++ b/src/JPEGWorker.cpp
@@ -51,8 +51,7 @@ bool JPEGWorker::ensure_running(int jpgChn) {
     return false;
   }
 
-  if (global_jpeg[jpgChn]->imp_encoder ||
-      global_jpeg[jpgChn]->running.load(std::memory_order_relaxed)) {
+  if (global_jpeg[jpgChn]->running.load(std::memory_order_acquire)) {
     return true;
   }
 
@@ -65,7 +64,11 @@ bool JPEGWorker::ensure_running(int jpgChn) {
   }
 
   sh.has_started.acquire();
-  return global_jpeg[jpgChn]->imp_encoder != nullptr;
+  if (!sh.success.load(std::memory_order_acquire)) {
+    pthread_join(global_jpeg[jpgChn]->thread, nullptr);
+    return false;
+  }
+  return true;
 }
 
 int JPEGWorker::save_jpeg_stream(int fd, IMPEncoderStream *stream) {
@@ -306,18 +309,22 @@ bool JPEGWorker::run() {
                              << diff_last_image << "ms size=" << total_size);
 
             if (global_jpeg[jpgChn]->stream->jpeg_refresh > 0) {
-              const char *tempPath = "/tmp/snapshot.tmp";
               const char *finalPath = global_jpeg[jpgChn]->stream->jpeg_path;
+              std::string tempPath = std::string(finalPath) + ".tmp.ch" +
+                                     std::to_string(jpgChn);
 
-              int snap_fd = open(tempPath, O_RDWR | O_CREAT | O_TRUNC, 0666);
+              int snap_fd = open(tempPath.c_str(), O_CLOEXEC | O_CREAT |
+                                                       O_NOFOLLOW | O_TRUNC |
+                                                       O_WRONLY,
+                                 0640);
               if (snap_fd >= 0) {
                 save_jpeg_stream(snap_fd, &stream);
                 close(snap_fd);
 
-                if (rename(tempPath, finalPath) != 0) {
+                if (rename(tempPath.c_str(), finalPath) != 0) {
                   LOG_ERROR("Failed to move JPEG snapshot from "
                             << tempPath << " to " << finalPath);
-                  std::remove(tempPath);
+                  std::remove(tempPath.c_str());
                 }
               } else {
                 LOG_ERROR(
@@ -490,17 +497,21 @@ void *JPEGWorker::thread_entry(void *arg) {
     return nullptr;
   }
 
-  // inform main that initialization is complete
-  sh->has_started.release();
-
   ret = IMP_Encoder_StartRecvPic(global_jpeg[jpgChn]->encChn);
   LOG_DEBUG_OR_ERROR(ret, "IMP_Encoder_StartRecvPic("
                               << global_jpeg[jpgChn]->encChn << ")");
-  if (ret != 0)
-    return 0;
+  if (ret != 0) {
+    global_jpeg[jpgChn]->imp_encoder->deinit(false);
+    delete global_jpeg[jpgChn]->imp_encoder;
+    global_jpeg[jpgChn]->imp_encoder = nullptr;
+    sh->has_started.release();
+    return nullptr;
+  }
 
   global_jpeg[jpgChn]->active = true;
   global_jpeg[jpgChn]->running = true;
+  sh->success.store(true, std::memory_order_release);
+  sh->has_started.release();
   JPEGWorker worker(jpgChn, impEncChn);
   bool encoder_receiving = worker.run();
 
diff --git a/src/WorkerUtils.hpp b/src/WorkerUtils.hpp
index f51a488..80d4055 100644
--- a/src/WorkerUtils.hpp
+++ b/src/WorkerUtils.hpp
@@ -8,6 +8,7 @@
 struct StartHelper {
   int encChn;
   binary_semaphore_compat has_started{0};
+  std::atomic<bool> success{false};
 };
 
 namespace WorkerUtils {
diff --git a/src/main.cpp b/src/main.cpp
index ab5daf3..acf4958 100644
--- a/src/main.cpp
+++ b/src/main.cpp
@@ -1021,8 +1021,12 @@ int main(int argc, const char *argv[]) {
             pthread_create(&global_jpeg[0]->thread, nullptr,
                            JPEGWorker::thread_entry, static_cast<void *>(&sh));
         LOG_DEBUG_OR_ERROR(ret, "create jpeg thread");
-        // wait for initialization done
-        sh.has_started.acquire();
+        if (ret == 0) {
+          sh.has_started.acquire();
+          if (!sh.success.load(std::memory_order_acquire)) {
+            pthread_join(global_jpeg[0]->thread, nullptr);
+          }
+        }
       }
 
       if (cfg->stream3.enabled &&
@@ -1032,7 +1036,12 @@ int main(int argc, const char *argv[]) {
             pthread_create(&global_jpeg[1]->thread, nullptr,
                            JPEGWorker::thread_entry, static_cast<void *>(&sh));
         LOG_DEBUG_OR_ERROR(ret, "create jpeg thread 2");
-        sh.has_started.acquire();
+        if (ret == 0) {
+          sh.has_started.acquire();
+          if (!sh.success.load(std::memory_order_acquire)) {
+            pthread_join(global_jpeg[1]->thread, nullptr);
+          }
+        }
       }
 
       if (cfg->osd.sei.enabled || cfg->osd.burnin.enabled) {
