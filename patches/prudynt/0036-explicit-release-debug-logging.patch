diff --git a/src/Config.hpp b/src/Config.hpp
index 8cbac78..64bbd41 100644
--- a/src/Config.hpp
+++ b/src/Config.hpp
@@ -14,8 +14,8 @@
 #include <sys/time.h>
 #include <vector>
 
-//~65k
-#define ENABLE_LOG_DEBUG
+// ENABLE_LOG_DEBUG is supplied by build.sh for explicit debug builds. Release
+// builds keep debug messages out while Logger.hpp preserves all error paths.
 
 // Some more debug output not useful for users (Developer Debug)
 // #define DDEBUG
diff --git a/src/Logger.hpp b/src/Logger.hpp
index 3d5c032..766a7fc 100644
--- a/src/Logger.hpp
+++ b/src/Logger.hpp
@@ -1,7 +1,9 @@
 #ifndef Logger_hpp
 #define Logger_hpp
 
+#if !defined(PRUDYNT_LOGGER_STANDALONE)
 #include "Config.hpp"
+#endif
 #include <cstring>
 #include <mutex>
 #include <sstream>
diff --git a/tests/run_host_tests.sh b/tests/run_host_tests.sh
index ab7b672..0553cd4 100755
--- a/tests/run_host_tests.sh
+++ b/tests/run_host_tests.sh
@@ -8,7 +8,8 @@ test_bin=$(mktemp "${TMPDIR%/}/prudynt-media-queue.XXXXXX")
 motion_bin=$(mktemp "${TMPDIR%/}/prudynt-motion-event.XXXXXX")
 isp_capability_bin=$(mktemp "${TMPDIR%/}/prudynt-isp-capability.XXXXXX")
 release_error_bin=$(mktemp "${TMPDIR%/}/prudynt-release-error.XXXXXX")
-trap 'rm -f "$test_bin" "$motion_bin" "$isp_capability_bin" "$release_error_bin"' EXIT HUP INT TERM
+debug_error_bin=$(mktemp "${TMPDIR%/}/prudynt-debug-error.XXXXXX")
+trap 'rm -f "$test_bin" "$motion_bin" "$isp_capability_bin" "$release_error_bin" "$debug_error_bin"' EXIT HUP INT TERM
 
 osd_start_owners=$(grep -l 'IMP_OSD_Start(' "$repo_dir"/src/*.cpp || true)
 test "$osd_start_owners" = "$repo_dir/src/IMPEncoder.cpp"
@@ -45,6 +46,13 @@ ${CXX:-c++} -std=c++20 -Wall -Wextra -Werror \
 "$isp_capability_bin"
 
 ${CXX:-c++} -std=c++20 -Wall -Wextra -Werror -pthread \
+  -DPRUDYNT_LOGGER_STANDALONE \
   -I"$repo_dir/src" "$repo_dir/tests/test_release_error_handling.cpp" \
   -o "$release_error_bin"
 "$release_error_bin"
+
+${CXX:-c++} -std=c++20 -Wall -Wextra -Werror -pthread \
+  -DPRUDYNT_LOGGER_STANDALONE -DENABLE_LOG_DEBUG \
+  -I"$repo_dir/src" "$repo_dir/tests/test_release_error_handling.cpp" \
+  -o "$debug_error_bin"
+"$debug_error_bin"
diff --git a/tests/test_release_error_handling.cpp b/tests/test_release_error_handling.cpp
index 33c1370..5334185 100644
--- a/tests/test_release_error_handling.cpp
+++ b/tests/test_release_error_handling.cpp
@@ -34,17 +34,30 @@ int main() {
   logged_levels.clear();
   LOG_DEBUG_OR_ERROR(evaluated(0), "successful operation");
   assert(condition_evaluations == 1);
+#if defined(ENABLE_LOG_DEBUG)
+  assert(logged_levels.size() == 1);
+  assert(logged_levels.back() == Logger::DEBUG);
+#else
   assert(logged_levels.empty());
+#endif
 
   LOG_DEBUG_OR_ERROR(evaluated(-3), "failed operation");
   assert(condition_evaluations == 2);
+#if defined(ENABLE_LOG_DEBUG)
+  assert(logged_levels.size() == 2);
+#else
   assert(logged_levels.size() == 1);
+#endif
   assert(logged_levels.back() == Logger::ERROR);
 
   assert(fatal_operation(0) == 0);
   assert(fatal_operation(-7) == -7);
   assert(condition_evaluations == 4);
+#if defined(ENABLE_LOG_DEBUG)
+  assert(logged_levels.size() == 4);
+#else
   assert(logged_levels.size() == 2);
+#endif
   assert(logged_levels.back() == Logger::ERROR);
 
   IMPSystemLifecycle lifecycle;
