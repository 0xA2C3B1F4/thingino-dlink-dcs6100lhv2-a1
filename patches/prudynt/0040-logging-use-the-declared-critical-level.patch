diff --git a/src/Logger.hpp b/src/Logger.hpp
index 766a7fc..7999f6c 100644
--- a/src/Logger.hpp
+++ b/src/Logger.hpp
@@ -11,7 +11,7 @@
 #define FILENAME (strrchr("/" __FILE__, '/') + 1)
 #define LOG_EMER(str) Logger::log(Logger::EMERGENCY, FILENAME, LogMsg() << str)
 #define LOG_ALER(str) Logger::log(Logger::ALERT, FILENAME, LogMsg() << str)
-#define LOG_CRIT(str) Logger::log(Logger::CRITICAL, FILENAME, LogMsg() << str)
+#define LOG_CRIT(str) Logger::log(Logger::CRIT, FILENAME, LogMsg() << str)
 #define LOG_ERROR(str) Logger::log(Logger::ERROR, FILENAME, LogMsg() << str)
 #define LOG_WARN(str) Logger::log(Logger::WARN, FILENAME, LogMsg() << str)
 #define LOG_NOTICE(str) Logger::log(Logger::NOTICE, FILENAME, LogMsg() << str)
diff --git a/tests/test_release_error_handling.cpp b/tests/test_release_error_handling.cpp
index b052a45..2a7be9e 100644
--- a/tests/test_release_error_handling.cpp
+++ b/tests/test_release_error_handling.cpp
@@ -75,6 +75,9 @@ int main() {
 #endif
   assert(logged_levels.back() == Logger::ERROR);
 
+  LOG_CRIT("critical startup failure");
+  assert(logged_levels.back() == Logger::CRIT);
+
   // The logging-only macro must remain usable in every return context used by
   // Prudynt. It must never inject an implicit return.
   logging_void(0);
