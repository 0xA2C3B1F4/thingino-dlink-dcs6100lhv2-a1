diff --git a/src/MP4ControlSocket.cpp b/src/MP4ControlSocket.cpp
index 8427dc5..74379b1 100644
--- a/src/MP4ControlSocket.cpp
+++ b/src/MP4ControlSocket.cpp
@@ -38,6 +38,7 @@ namespace {
 namespace fs = std::filesystem;
 constexpr const char *FIFO_DIR = "/run/prudynt";
 constexpr const char *FIFO_PATH = "/run/prudynt/mp4ctl";
+std::atomic<bool> recorder_shutdown_requested{false};
 
 std::string channel_state_path(int channel) {
   char buffer[64];
@@ -46,24 +47,53 @@ std::string channel_state_path(int channel) {
   return std::string(buffer);
 }
 
-void write_channel_state_file(int channel, const std::string &record_path,
+bool write_state_payload(int fd, const std::string &payload) {
+  size_t offset = 0;
+  while (offset < payload.size()) {
+    ssize_t written = ::write(fd, payload.data() + offset,
+                              payload.size() - offset);
+    if (written < 0 && errno == EINTR) {
+      continue;
+    }
+    if (written <= 0) {
+      return false;
+    }
+    offset += static_cast<size_t>(written);
+  }
+  return true;
+}
+
+bool write_channel_state_file(int channel, const std::string &record_path,
                               int duration_seconds) {
   if (channel < 0 || channel >= NUM_VIDEO_CHANNELS) {
-    return;
+    return false;
   }
   auto state_path = channel_state_path(channel);
-  int fd = ::open(state_path.c_str(), O_CREAT | O_WRONLY | O_TRUNC, 0644);
+  auto temporary_path = state_path + ".tmp";
+  ::unlink(temporary_path.c_str());
+  int fd = ::open(temporary_path.c_str(),
+                  O_CREAT | O_EXCL | O_WRONLY | O_CLOEXEC | O_NOFOLLOW, 0644);
   if (fd < 0) {
     LOG_WARN("MP4ControlSocket: failed to update state file " << state_path);
-    return;
+    return false;
   }
   std::string payload = "path=" + record_path + "\n";
   if (duration_seconds > 0) {
     payload += "duration=" + std::to_string(duration_seconds) + "\n";
   }
-  ssize_t ignored = ::write(fd, payload.c_str(), payload.size());
-  (void)ignored;
-  ::close(fd);
+  bool ok = write_state_payload(fd, payload);
+  if (ok && ::fsync(fd) != 0) {
+    ok = false;
+  }
+  if (::close(fd) != 0) {
+    ok = false;
+  }
+  if (ok && ::rename(temporary_path.c_str(), state_path.c_str()) == 0) {
+    return true;
+  }
+  ::unlink(temporary_path.c_str());
+  LOG_WARN("MP4ControlSocket: failed to publish state file " << state_path);
+  return false;
 }
 
 void remove_channel_state_file(int channel) {
@@ -1100,6 +1130,10 @@ bool wait_for_recording_completion(int channel,
 }
 
 bool begin_segment(const std::string &path, int channel, int duration_seconds) {
+  if (recorder_shutdown_requested.load(std::memory_order_acquire)) {
+    LOG_WARN("MP4ControlSocket: refusing segment start during shutdown");
+    return false;
+  }
   if (path.empty()) {
     LOG_ERROR("MP4ControlSocket: cannot START without a target path");
     return false;
@@ -1114,7 +1148,11 @@ bool begin_segment(const std::string &path, int channel, int duration_seconds) {
   if (!ok) {
     return false;
   }
-  write_channel_state_file(channel, path, duration_seconds);
+  if (!write_channel_state_file(channel, path, duration_seconds)) {
+    LOG_ERROR("MP4ControlSocket: recorder marker transaction failed");
+    stop_recording(channel);
+    return false;
+  }
   if (duration_seconds > 0) {
     schedule_stop_timer(channel, duration_seconds);
   }
@@ -1236,6 +1274,13 @@ void MP4ControlSocket::retireFailedRecording(int channel,
   stop_recording_locked(channel);
 }
 
+void MP4ControlSocket::stopAll() {
+  recorder_shutdown_requested.store(true, std::memory_order_release);
+  stop_all_loops();
+  cancel_all_stop_timers();
+  stop_all_recordings();
+}
+
 void MP4ControlSocket::run() {
   LOG_INFO("MP4ControlSocket: run() entered");
 
@@ -1282,6 +1327,10 @@ void MP4ControlSocket::run() {
       LOG_INFO("MP4ControlSocket: parsed op='" << op << "'");
 
       if (op == "START") {
+        if (recorder_shutdown_requested.load(std::memory_order_acquire)) {
+          LOG_WARN("MP4ControlSocket: rejecting START during shutdown");
+          continue;
+        }
         StartCommandOptions options;
         options.channel = default_recorder_channel();
         std::vector<std::string> positional;
@@ -1451,8 +1500,6 @@ void MP4ControlSocket::run() {
     ::close(fd);
   }
 
-  stop_all_loops();
-  cancel_all_stop_timers();
-  stop_all_recordings();
+  MP4ControlSocket::stopAll();
   ::unlink(FIFO_PATH);
 }
diff --git a/src/MP4ControlSocket.hpp b/src/MP4ControlSocket.hpp
index 3a5406b..76fbcad 100644
--- a/src/MP4ControlSocket.hpp
+++ b/src/MP4ControlSocket.hpp
@@ -13,5 +13,6 @@
 class MP4ControlSocket {
 public:
   static void retireFailedRecording(int channel, uint64_t generation);
+  static void stopAll();
   static void run();
 };
diff --git a/src/main.cpp b/src/main.cpp
index 1cbc374..dd970b7 100644
--- a/src/main.cpp
+++ b/src/main.cpp
@@ -1118,8 +1118,15 @@ int main(int argc, const char *argv[]) {
     lck.unlock();
 
 #if defined(DCS_DLINK_MEDIA_READY)
-    switch (dlinkMediaProcessAction(shutting_down, global_restart_video,
-                                    global_restart_audio)) {
+    DLinkMediaProcessAction process_action = dlinkMediaProcessAction(
+        shutting_down, global_restart_video, global_restart_audio);
+    if (process_action != DLinkMediaProcessAction::Continue) {
+      // Finish the current MP4 and retire its active marker while encoder
+      // workers still exist. The D-Link path exits or execs immediately after
+      // this and deliberately avoids the unsafe vendor SDK teardown.
+      MP4ControlSocket::stopAll();
+    }
+    switch (process_action) {
     case DLinkMediaProcessAction::Exit:
       LOG_INFO("D-Link shutdown: exiting without unsafe SDK teardown");
       _exit(0);
@@ -1222,6 +1229,8 @@ int main(int argc, const char *argv[]) {
 
   join_signal_thread(false);
 
+  MP4ControlSocket::stopAll();
+
   /* daynight thread join removed — photosensing delegated to daynightd */
 
 #if defined(WEBSOCKET_ENABLED)
diff --git a/tests/run_host_tests.sh b/tests/run_host_tests.sh
index 49f4a95..d1429e4 100755
--- a/tests/run_host_tests.sh
+++ b/tests/run_host_tests.sh
@@ -20,6 +20,11 @@ ready_release_line=$(grep -n 'sh->success.store(true' \
   "$repo_dir/src/VideoWorker.cpp" | cut -d: -f1)
 test "$ready_release_line" -gt "$start_recv_line"
 grep -q 'if (video_ready && !privacy_control_started)' "$repo_dir/src/main.cpp"
+grep -q 'if (!write_channel_state_file(channel, path, duration_seconds))' \
+  "$repo_dir/src/MP4ControlSocket.cpp"
+grep -q 'MP4ControlSocket::stopAll();' "$repo_dir/src/main.cpp"
+grep -q '::rename(temporary_path.c_str(), state_path.c_str())' \
+  "$repo_dir/src/MP4ControlSocket.cpp"
 
 ${CXX:-c++} -std=c++20 -Wall -Wextra -Werror -pthread \
   -I"$repo_dir/src" "$repo_dir/tests/test_media_queue.cpp" -o "$test_bin"
-- 
2.50.1

