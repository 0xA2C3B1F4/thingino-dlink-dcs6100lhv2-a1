diff --git a/cgi.c b/cgi.c
index 23072c8..c001d7a 100644
--- a/cgi.c
+++ b/cgi.c
@@ -45,29 +45,13 @@ void uh_interpreter_add(const char *ext, const char *path)
 
 static void cgi_main(struct client *cl, struct path_info *pi, char *url)
 {
-	const struct interpreter *ip = pi->ip;
-	struct env_var *var;
-
-	clearenv();
-	setenv("PATH", conf.cgi_path, 1);
-
-	for (var = uh_get_process_vars(cl, pi); var->name; var++) {
-		if (!var->value)
-			continue;
-
-		setenv(var->name, var->value, 1);
-	}
-
-	if (!chdir(pi->root)) {
-		if (ip)
-			execl(ip->path, ip->path, pi->phys, NULL);
-		else
-			execl(pi->phys, pi->phys, NULL);
-	}
+	(void) cl;
+	(void) pi;
+	(void) url;
+	errno = ENOSYS;
 
 	printf("Status: 500 Internal Server Error\r\n\r\n"
-	       "Unable to launch the requested CGI program:\n"
-	       "  %s: %s\n", ip ? ip->path : pi->phys, strerror(errno));
+	       "CGI execution is disabled\n");
 }
 
 static void cgi_handle_request(struct client *cl, char *url, struct path_info *pi)
diff --git a/control_proxy.c b/control_proxy.c
index 914f709..29d2874 100644
--- a/control_proxy.c
+++ b/control_proxy.c
@@ -19,8 +19,10 @@
 #define CONTROL_PROXY_HOST "127.0.0.1"
 #define CONTROL_PROXY_PORT 1998
 #define CONTROL_MEDIA_PORT 8080
+#define CONTROL_ONVIF_PORT 1999
 #define CONTROL_PROXY_TIMEOUT_MS 4000
 #define CONTROL_PROXY_MAX_BODY 4096
+#define CONTROL_ONVIF_MAX_BODY (16 * 1024)
 #define CONTROL_PROXY_MAX_RESPONSE (2 * 1024 * 1024)
 #define CONTROL_PROXY_WRITE_BUFFER 8192
 #define CONTROL_LOCAL_FILE_PATH_MAX 512
@@ -143,6 +145,14 @@ static bool control_proxy_status(struct dispatch_control_proxy *proxy, char *lin
 	return true;
 }
 
+static bool control_proxy_content_type(const char *value, const char *expected)
+{
+	size_t length = strlen(expected);
+
+	return !strncasecmp(value, expected, length) &&
+	       (!value[length] || value[length] == ';');
+}
+
 static bool control_proxy_response_header(struct client *cl, char *line)
 {
 	struct dispatch_control_proxy *proxy = &cl->dispatch.control_proxy;
@@ -168,11 +178,17 @@ static bool control_proxy_response_header(struct client *cl, char *line)
 
 	if (!strcasecmp(line, "Content-Type")) {
 		if (proxy->content_type_seen ||
-		    ((strncasecmp(value, "application/json", 16) ||
-		      (value[16] && value[16] != ';')) &&
-		     strcasecmp(value, "image/jpeg") &&
-		     (!proxy->media || strncasecmp(value,
-		       "multipart/x-mixed-replace", 25))))
+		    ((!proxy->onvif &&
+		      !control_proxy_content_type(value, "application/json") &&
+		      !control_proxy_content_type(value, "image/jpeg") &&
+		      (!proxy->media || !control_proxy_content_type(value,
+		        "multipart/x-mixed-replace"))) ||
+		     (proxy->onvif &&
+		      !control_proxy_content_type(value, "application/soap+xml") &&
+		      !control_proxy_content_type(value, "application/xml") &&
+		      !control_proxy_content_type(value, "text/xml") &&
+		      !control_proxy_content_type(value, "text/html") &&
+		      !control_proxy_content_type(value, "text/plain"))))
 			return false;
 		proxy->content_type_seen = true;
 		blobmsg_add_string(&proxy->hdr, "Content-Type", value);
@@ -207,6 +223,15 @@ static bool control_proxy_response_header(struct client *cl, char *line)
 		return true;
 	}
 
+	if (!strcasecmp(line, "WWW-Authenticate")) {
+		if (!proxy->onvif || proxy->www_authenticate_seen || !*value ||
+		    strlen(value) > 255 || strpbrk(value, "\r\n"))
+			return false;
+		proxy->www_authenticate_seen = true;
+		blobmsg_add_string(&proxy->hdr, "WWW-Authenticate", value);
+		return true;
+	}
+
 	if (!strcasecmp(line, "Transfer-Encoding"))
 		return false;
 
@@ -557,6 +582,7 @@ static void control_proxy_reset_response(struct dispatch_control_proxy *proxy)
 	proxy->content_disposition_seen = false;
 	proxy->location_seen = false;
 	proxy->set_cookie_seen = false;
+	proxy->www_authenticate_seen = false;
 	proxy->headers_done = false;
 	proxy->stream_until_eof = false;
 	blob_buf_free(&proxy->hdr);
@@ -652,7 +678,16 @@ static bool control_proxy_encode_target(const char *input, char *output,
 static bool control_proxy_url(const char *url)
 {
 	return !strncmp(url, "/api/v1/", 8) || !strcmp(url, "/api/v1") ||
-	       !strncmp(url, "/media/v1/", 10);
+	       !strncmp(url, "/media/v1/", 10) ||
+	       !strcmp(url, "/onvif/device_service") ||
+	       !strcmp(url, "/onvif/deviceio_service") ||
+	       !strcmp(url, "/onvif/events_service") ||
+	       !strcmp(url, "/onvif/imaging_service") ||
+	       !strcmp(url, "/onvif/media_service") ||
+	       !strcmp(url, "/onvif/media2_service") ||
+	       !strcmp(url, "/onvif/ptz_service") ||
+	       !strcmp(url, "/onvif/image.cgi") ||
+	       !strcmp(url, "/onvif/image1.cgi");
 }
 
 static void control_proxy_request(struct client *cl, char *url,
@@ -662,7 +697,8 @@ static void control_proxy_request(struct client *cl, char *url,
 	const char *authorization, *cookie, *api_key, *accept, *content_type;
 	char remote[INET6_ADDRSTRLEN];
 	const void *remote_address;
-	bool media;
+	bool media, onvif, onvif_snapshot;
+	int backend_port;
 	char encoded_target[PATH_MAX * 3];
 	(void) pi;
 
@@ -671,13 +707,19 @@ static void control_proxy_request(struct client *cl, char *url,
 	cl->dispatch.free = control_proxy_free;
 	proxy->finish.cb = control_proxy_finish;
 	media = control_proxy_is_media_url(url);
+	onvif = !strncmp(url, "/onvif/", 7);
+	onvif_snapshot = !strcmp(url, "/onvif/image.cgi") ||
+		!strcmp(url, "/onvif/image1.cgi");
 	proxy->media = media;
+	proxy->onvif = onvif && !onvif_snapshot;
+	proxy->onvif_snapshot = onvif_snapshot;
 
 	if (cl->request.transfer_chunked) {
 		control_proxy_fail(cl, 400, "Bad Request", "bad_request");
 		return;
 	}
-	if (cl->request.content_length > CONTROL_PROXY_MAX_BODY) {
+	if (cl->request.content_length > (onvif && !onvif_snapshot ?
+	    CONTROL_ONVIF_MAX_BODY : CONTROL_PROXY_MAX_BODY)) {
 		control_proxy_fail(cl, 413, "Payload Too Large", "payload_too_large");
 		return;
 	}
@@ -688,6 +730,16 @@ static void control_proxy_request(struct client *cl, char *url,
 		control_proxy_fail(cl, 400, "Bad Request", "bad_media_request");
 		return;
 	}
+	if (onvif_snapshot && (cl->request.method != UH_HTTP_MSG_GET ||
+	    cl->request.content_length != 0)) {
+		control_proxy_fail(cl, 405, "Method Not Allowed", "method_not_allowed");
+		return;
+	}
+	if (onvif && !onvif_snapshot &&
+	    cl->request.method != UH_HTTP_MSG_POST) {
+		control_proxy_fail(cl, 405, "Method Not Allowed", "method_not_allowed");
+		return;
+	}
 	if (cl->request.method != UH_HTTP_MSG_GET &&
 	    cl->request.method != UH_HTTP_MSG_POST &&
 	    cl->request.method != UH_HTTP_MSG_PUT &&
@@ -696,7 +748,9 @@ static void control_proxy_request(struct client *cl, char *url,
 		return;
 	}
 
-	if (control_proxy_start_backend(cl, CONTROL_PROXY_PORT)) {
+	backend_port = onvif && !onvif_snapshot ? CONTROL_ONVIF_PORT :
+		CONTROL_PROXY_PORT;
+	if (control_proxy_start_backend(cl, backend_port)) {
 		control_proxy_fail(cl, 502, "Bad Gateway", "bad_gateway");
 		return;
 	}
@@ -718,12 +772,15 @@ static void control_proxy_request(struct client *cl, char *url,
 			"GET /api/v1/internal/media-authorize?target=%s HTTP/1.1\r\n"
 			"Host: %s:%d\r\nConnection: close\r\nContent-Length: 0\r\n",
 			encoded_target, CONTROL_PROXY_HOST, CONTROL_PROXY_PORT);
+		if (onvif_snapshot)
+			ustream_printf(&proxy->backend.stream,
+				"X-Thingino-Proxy: 2\r\n");
 	} else {
 		ustream_printf(&proxy->backend.stream,
 			"%s %s HTTP/1.1\r\nHost: %s:%d\r\nConnection: close\r\n"
 			"Content-Length: %d\r\n",
 			http_methods[cl->request.method], url,
-			CONTROL_PROXY_HOST, CONTROL_PROXY_PORT,
+			CONTROL_PROXY_HOST, backend_port,
 			cl->request.content_length);
 	}
 
@@ -739,18 +800,23 @@ static void control_proxy_request(struct client *cl, char *url,
 		ustream_printf(&proxy->backend.stream, "Cookie: %s\r\n", cookie);
 	if (api_key)
 		ustream_printf(&proxy->backend.stream, "X-API-Key: %s\r\n", api_key);
-	if (!media && accept)
+	if (!media && !onvif_snapshot && accept)
 		ustream_printf(&proxy->backend.stream, "Accept: %s\r\n", accept);
-	if (!media && content_type)
+	if (!media && !onvif_snapshot && content_type)
 		ustream_printf(&proxy->backend.stream, "Content-Type: %s\r\n",
 			content_type);
 
 	remote_address = cl->peer_addr.family == AF_INET6 ?
 		(const void *) &cl->peer_addr.in6 : (const void *) &cl->peer_addr.in;
-	if (inet_ntop(cl->peer_addr.family, remote_address, remote, sizeof(remote)))
-		ustream_printf(&proxy->backend.stream,
-			"X-Thingino-Proxy: 1\r\nX-Thingino-Remote-Addr: %s\r\n",
-			remote);
+	if (inet_ntop(cl->peer_addr.family, remote_address, remote, sizeof(remote))) {
+		if (onvif && !onvif_snapshot)
+			ustream_printf(&proxy->backend.stream,
+				"X-Thingino-Remote-Addr: %s\r\n", remote);
+		else if (!onvif_snapshot)
+			ustream_printf(&proxy->backend.stream,
+				"X-Thingino-Proxy: 1\r\nX-Thingino-Remote-Addr: %s\r\n",
+				remote);
+	}
 	ustream_printf(&proxy->backend.stream, "\r\n");
 }
 
diff --git a/main.c b/main.c
index 542e890..1ae146a 100644
--- a/main.c
+++ b/main.c
@@ -317,7 +317,6 @@ int main(int argc, char **argv)
 	BUILD_BUG_ON(sizeof(uh_buf) < PATH_MAX);
 
 	uh_dispatch_add(&control_proxy_dispatch);
-	uh_dispatch_add(&cgi_dispatch);
 	init_defaults_pre();
 	signal(SIGPIPE, SIG_IGN);
 
diff --git a/proc.c b/proc.c
index fc8a325..d5221e5 100644
--- a/proc.c
+++ b/proc.c
@@ -180,6 +180,7 @@ struct env_var *uh_get_process_vars(struct client *cl, struct path_info *pi)
 	return vars;
 }
 
+#if 0 /* D-Link production build: legacy request-process relay is excluded. */
 static void proc_close_fds(struct client *cl)
 {
 	struct dispatch_proc *p = &cl->dispatch.proc;
@@ -336,74 +337,17 @@ static void proc_timeout_cb(struct uloop_timeout *timeout)
 
 	uh_relay_kill(cl, &proc->r);
 }
+#endif
 
 bool uh_create_process(struct client *cl, struct path_info *pi, char *url,
 		       void (*cb)(struct client *cl, struct path_info *pi, char *url))
 {
-	struct dispatch *d = &cl->dispatch;
-	struct dispatch_proc *proc = &d->proc;
-	int rfd[2], wfd[2];
-	int pid;
-
-	blob_buf_init(&proc->hdr, 0);
-	proc->status_code = 200;
-	strcpy(proc->status_msg, "OK");
-
-	if (pipe(rfd))
-		return false;
-
-	if (pipe(wfd))
-		goto close_rfd;
-
-	pid = fork();
-	if (pid < 0)
-		goto close_wfd;
-
-	if (!pid) {
-		close(0);
-		close(1);
-
-		dup2(rfd[1], 1);
-		dup2(wfd[0], 0);
-
-		close(rfd[0]);
-		close(rfd[1]);
-		close(wfd[0]);
-		close(wfd[1]);
-
-		uh_close_fds();
-		signal(SIGPIPE, SIG_DFL);
-		cb(cl, pi, url);
-		exit(0);
-	}
-
-	close(rfd[1]);
-	close(wfd[0]);
-
-	proc->wrfd.fd = wfd[1];
-	uh_relay_open(cl, &proc->r, rfd[0], pid);
-
-	d->free = proc_free;
-	d->close_fds = proc_close_fds;
-	d->data_send = proc_data_send;
-	d->data_done = proc_write_close;
-	d->write_cb = proc_relay_write_cb;
-	proc->r.header_cb = proc_handle_header;
-	proc->r.header_end = proc_handle_header_end;
-	proc->r.close = proc_handle_close;
-	proc->wrfd.cb = proc_write_cb;
-	proc->timeout.cb = proc_timeout_cb;
-	if (conf.script_timeout > 0)
-		uloop_timeout_set(&proc->timeout, conf.script_timeout * 1000);
-
-	return true;
-
-close_wfd:
-	close(wfd[0]);
-	close(wfd[1]);
-close_rfd:
-	close(rfd[0]);
-	close(rfd[1]);
-
+	(void) cl;
+	(void) pi;
+	(void) url;
+	(void) cb;
+	/* Preserve the legacy plugin ABI but make request process creation
+	 * structurally impossible in the D-Link production ingress. */
+	errno = ENOSYS;
 	return false;
 }
diff --git a/uhttpd.h b/uhttpd.h
index 116213f..956e320 100644
--- a/uhttpd.h
+++ b/uhttpd.h
@@ -234,9 +234,12 @@ struct dispatch_control_proxy {
 	bool content_disposition_seen;
 	bool location_seen;
 	bool set_cookie_seen;
+	bool www_authenticate_seen;
 	bool headers_done;
 	bool finishing;
 	bool media;
+	bool onvif;
+	bool onvif_snapshot;
 	bool authorizing;
 	bool local_file;
 	bool local_file_download;
